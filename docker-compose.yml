services:
  gobox-mcp:
    build: .
    container_name: gobox-mcp
    ports:
      - "8000:8000"
    env_file: .env
    environment:
      - MCP_TRANSPORT=sse
      - PORT=8000
    restart: unless-stopped
